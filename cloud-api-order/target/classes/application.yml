server:
  port: 7002

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
  instance:
    instance-id: order-service
    prefer-ip-address: true